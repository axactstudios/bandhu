class Activity {
  String name, rawMaterial, avgProduction, key;
  List imageList, videoList;
  Activity(
      {this.name,
      this.rawMaterial,
      this.avgProduction,
      this.key,
      this.imageList,
      this.videoList});
}
